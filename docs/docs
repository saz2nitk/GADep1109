Deployment
-- aws
	-- account creation
	-- ec2 creation
	
Open a python editor
	- Pycharm
	- VSCode
	- Spyder
	- Sublime

Github
	- acount created
	- git installed on our devloper system

Coding practices
	- modular codes
	- use of classes and functions
	- project structure
	- variable/def/classes/modules naming
	- pep 8 and pep 257
	- You should always have a plan
		- findALl classes
		- getContent from each classes
		- Collect the data and dump into csv
	- use virtual env
	
Api development
	- Flask
	- install postman
	
git init
git add .
git commit -m "some message"

ssh -i locationofpemfile ubuntu@ipaddress
sudo apt-get update
sudo apt-get install python3-pip
pip3 install flask
ls -- to list dir
pwd -- current wd
mkdir dirname --- create new dir
cd dirname -- to change dir
cd .. --- to go one dir up
git clone https://github.com/saz2nitk/GADep1109.git

when we make REST api call, it is an http request

sudo su
scp -i locationofpemfile source dest
 scp -i /c/Users/saz2n/Downloads/ga1209test.pem /d/Academia/Learning/GreyAtom/GADep1109/
	
